import { DatePicker } from 'antd';
import moment from 'moment';
import './index.scss'
const { RangePicker } = DatePicker
export const FakeDatePicker = () => {
    return <div className='fake-date-picker'>
    
    </div>
}